[project]
name = "graph-agent"
version = "0.1.0"
description = "Graph agent package."
readme = "README.md"
requires-python = ">=3.12"
license = "GPL-3.0-only"
dependencies = [
    "openai>=1.66.0",
]

[build-system]
requires = ["uv_build>=0.11.19,<0.12"]
build-backend = "uv_build"
